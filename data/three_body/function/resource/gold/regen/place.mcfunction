# Place 금(gold) resource node
# TODO: map coordinates for gold are not yet decided (told to Claude as "미정").
# Once the node position and block id are fixed, mirror stone/regen/place.mcfunction:
#   setblock <x> <y> <z> <block> replace
#   scoreboard players reset #gold_remain generate
