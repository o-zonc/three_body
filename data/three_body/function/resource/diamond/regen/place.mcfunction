# Place 다이아몬드(diamond) resource node
# TODO: map coordinates for diamond are not yet decided (told to Claude as "미정").
# Once the node position and block id are fixed, mirror stone/regen/place.mcfunction:
#   setblock <x> <y> <z> <block> replace
#   scoreboard players reset #diamond_remain generate
